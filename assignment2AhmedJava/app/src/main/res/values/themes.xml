<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Assignment2AhmedJava" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Set primary colors to black -->
        <item name="colorPrimary">@android:color/black</item>
        <item name="colorPrimaryDark">@android:color/black</item>
        <item name="colorAccent">@android:color/black</item>

        <!-- Optionally, set additional Material3-specific color attributes -->
        <item name="colorPrimaryContainer">@android:color/black</item>
        <item name="colorSecondary">@android:color/black</item>
    </style>

    <style name="Theme.Assignment2AhmedJava" parent="Base.Theme.Assignment2AhmedJava" />
</resources>
